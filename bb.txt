'@group(0) @binding(12) var<storage, read> tuvs : array<vec4f>;    // per-vertex uv\n@group(0) @binding(13) var atlasTex : binding_array<texture_2d<f32>, 8>;\n@group(0) @binding(14) var texSamp : sampler;\n@group(0) @binding(15) var<storage, read> matUVs : array<vec4f>;  // 2 per material: [u0,v0,su,sv] [page,hasTex,0,0]\n\nfn aabb_hit(bmin: vec3f, bmax: vec3f, ro: vec3f, inv: vec3f, tmax: f32) -> bool {\n  let t0 = (bmin - ro) * inv;\n  let t1 = (bmax - ro) * inv;\n  let tmin_i = max(max(min(t0.x, t1.x), min(t0.y, t1.y)), min(t0.z, t1.z));\n  let tmax_i = min(min'
